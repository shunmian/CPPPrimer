#include <iostream>
using namespace std;
int main() {
    cout << "Hello world " << endl;
    /* `<< endl;` is incorrect since << requires /* a nested comment */ a left operand cout. */
    return 0;
}